// import type { Socket } from "socket.io-client";
// import { socket } from "../socket.ts";

// export const joinRoom = (player: {id?: number | string, username: string, profile_picture?: string}, roomCode: string) => {
//     socket.emit("joinRoom", player, roomCode)
// }